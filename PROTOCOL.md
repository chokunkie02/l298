# โปรโตคอล Serial ระหว่าง Host กับ ESP32 (Braille 6 จุด, หนึ่งเซลล์)

> เอกสารนี้บันทึก **เฉพาะสิ่งที่ยืนยันได้จากซอร์สโค้ดใน repo นี้** ค่าที่ยัง
> ไม่ได้รับการยืนยัน (เฟิร์มแวร์, GPIO, ACK, ความปลอดภัยเชิงความร้อน) ถูกทำ
> เครื่องหมายว่า **ยังไม่ยืนยัน** — ห้ามนำไปอ้างเป็นข้อเท็จจริง

## 1. Host payload (ยืนยันแล้ว)

| รายการ | ค่า | หลักฐานใน repo |
| --- | --- | --- |
| เนื้อหา | อักขระ ASCII `'0'` / `'1'` จำนวน **6 ตัวพอดี** เรียงตามลำดับจุด 1,2,3,4,5,6 | `app.py` `/send`; `braille_models.bit_pattern_from_bitmask` |
| ตัวคั่นท้าย | LF หนึ่งไบต์ (`0x0A`, `"\n"`) | `app.py` `f"{pattern}\n".encode("utf-8")` |
| ความยาว payload | **7 ไบต์เสมอ** | `braille_hardware.encode_pattern` |
| ตัวอย่าง | `101010` → `b"101010\n"` (เปิดจุด 1,3,5) | `tests/test_braille_hardware.py` |
| baud rate | **115200** | `app.DEFAULT_BAUD` |
| framing | 8N1 (ค่าเริ่มต้น `serial.Serial`) | `app.init_serial` |
| คำสั่งล้างเซลล์ | `000000` + LF = `b"000000\n"` | `braille_hardware.CLEAR_PATTERN` |

ระบบ **ไม่เคยส่ง**: ข้อความ OCR, Unicode Braille (U+28xx), JSON, สตริงยาวผิด
ขนาด, หรือรูปแบบที่ไม่ตรง `^[01]{6}$` — ทุกอย่างถูก validate ก่อนเข้ารหัส

## 2. การตอบกลับจากอุปกรณ์ (ACK)

**ไม่มี.** ไม่มีการเรียก `.read()` / `.readline()` / `in_waiting` ที่ใดในซอร์ส
(`timeout=1` ถูกตั้งไว้แต่ไม่เคยใช้อ่าน) ดังนั้น:

- `acknowledged_by_device` = **unknown เสมอ**
- `physically_displayed` = **unknown เสมอ**
- "เขียน byte สำเร็จ" **ไม่ได้แปลว่า** เซลล์ถูกแสดงผลบนฮาร์ดแวร์

ห้ามสร้าง ACK handshake ปลอมเพื่อให้ UI ดู "สำเร็จ"

## 3. ความหมายสี่ระดับของ "สำเร็จ"

```
accepted_by_server   →  ผ่าน validation ฝั่ง Flask
written_to_serial    →  ser.write(payload) + ser.flush() คืนค่าปกติ
acknowledged_by_device →  null  (ไม่มี ACH parsing)
physically_displayed →  null  (พิสูจน์ไม่ได้จาก host)
```

ข้อความ UI มาตรฐานหลังเขียน: **"ส่งคำสั่งผ่าน Serial แล้ว แต่ยังไม่ได้รับการ
ยืนยันจากอุปกรณ์"**

## 4. เซลล์ว่างจริง vs ช่องว่างชั่วคราว

| กรณี | ส่งอะไร | เพิ่ม real-cell index? |
| --- | --- | --- |
| เซลล์ว่างจริง (มี index ในลำดับ เช่น เว้นวรรคระหว่างคำ) | `000000\n` | **ใช่** |
| ช่องว่างชั่วคราวระหว่างเซลล์สองเซลล์ (inter-cell gap) | `000000\n` | **ไม่** |
| ช่วงหยุดขึ้นบรรทัดใหม่ (line pause) | `000000\n` | **ไม่** |

ทั้งสามกรณีส่ง byte เดียวกัน แต่ความหมายเชิงลำดับต่างกัน — session manager แยก
สองสิ่งนี้ด้วยพารามิเตอร์ `transient_gap` vs `real_cell_index`

## 4b. การจัดคิวคำขอฝั่ง client + generation

ทุกคำขอที่เปลี่ยนสถานะฮาร์ดแวร์ (`display cell`, `transient gap`, `verify`) ของ
เซสชันหนึ่งเข้าคิวใน `static/braille_hardware.js` และทำ **ทีละคำขอ** เท่านั้น:

- ทุก response ที่สำเร็จคืน `generation` ใหม่ คำขอถัดไปในคิวใช้ค่านั้น
- ไม่มีทางที่สองคำขอออกพร้อมกันด้วย `generation` เดียวกัน → ไม่เกิด
  `409 stale_session` ระหว่างการเล่นตามลำดับปกติ
- `stopSession` ไม่เข้าคิว: ทำให้เซสชัน client เป็นโมฆะทันที (บวก `epoch`) ล้าง
  คิว แล้วส่ง `POST /api/hardware/playback/stop` ทันที
- เส้นทาง `/stop` ฝั่งเซิร์ฟเวอร์เป็น **idempotent** และตรวจเฉพาะ `session_id`
  (ไม่ตรวจ `generation`) — `generation` ที่ล้าสมัยจึงไม่ขวางการล้างเซสชันที่
  `session_id` ยังตรงกัน
- response ที่มาถึงช้าหลัง stop/restart ถูกทิ้ง (`epoch` ไม่ตรง) — **ห้ามปลุก
  เซสชันใหม่ให้ทำงาน**

## 4c. นโยบาย Pause (สอดคล้องทุกที่)

ปุ่ม Pause / "ฟังอีกครั้ง" / Stop / Restart / OCR ใหม่ / แปลใหม่ / ปิดโหมด
ฮาร์ดแวร์ → **จบและล้างเซสชันฮาร์ดแวร์เสมอ** (ส่ง `000000\n` หนึ่งครั้งผ่าน
`/stop`) การเล่นต่อต้องกด "เริ่มเซสชันฮาร์ดแวร์" ใหม่ ปุ่มเล่นหลักไม่เริ่มเซสชัน
อัตโนมัติ

## 5. host watchdog (ฝั่ง Flask)

- ทุกคำขอเซลล์ที่ถูกต้อง refresh เส้นตาย (`DEFAULT_WATCHDOG_SECONDS = 4.0`s,
  ปรับได้ตอน `start()`, clamp 1–30s) — 4.0s เป็นค่าอนุรักษ์นิยมฝั่ง host เท่านั้น
  **ไม่ใช่ค่าที่ยืนยันว่าปลอดภัยเชิงกายภาพ** (commit `e45bdd5` เคยดันเป็น 10.0s
  โดยไม่มีหลักฐาน จึง revert)
- ตั้ง `cellDurationMs` ≤ watchdog เพื่อการเล่นฮาร์ดแวร์ต่อเนื่อง (ค่าปกติ 3000ms
  < 4000ms); ถ้าเกิน watchdog จะล้างเซลล์และจบเซสชันระหว่างเซลล์ (fail-safe)
- เลยเส้นตาย → ยกเลิกเซสชัน → best-effort ส่ง `000000\n` → บันทึก safety event
  (ไม่มีข้อความ OCR)
- **ป้องกันไม่ได้**: เครื่องแครช, USB หลุด, ไฟดับ — พฤติกรรม watchdog ระดับ
  เฟิร์มแวร์ยังไม่ทราบ

## 6. สิ่งที่ยังไม่ยืนยัน (ต้องถามทีมฮาร์ดแวร์)

- การจับคู่จุด 1–6 ↔ ขา GPIO ของ ESP32 ↔ ช่องของ L298N — **ไม่ทราบ**
- เฟิร์มแวร์ตีความ `000000` ว่า "ปลดพลังงานทุกจุด" หรือไม่ — **สันนิษฐาน**
- เวลาค้างเซลล์สูงสุดก่อน actuator/L298N ร้อนเกิน — **ไม่ทราบ**
- เฟิร์มแวร์มี watchdog / auto-clear timeout หรือไม่ — **ไม่ทราบ** (ถ้าไม่มี =
  blocker สำหรับใช้งานแบบไม่มีคนดูแล)
- รูปแบบ/เงื่อนไขของ ACK ถ้ามี — **ไม่ทราบ**
- พฤติกรรมเมื่อ USB หลุดกลางคัน — **ไม่ทราบ**
- เวอร์ชันเฟิร์มแวร์ปัจจุบัน และตำแหน่งซอร์ส — **ไม่ทราบ** (ไม่มีใน repo)

## 7. โหมดตรวจสอบลำดับจุดด้วยมือ

`POST /api/hardware/verify/cell` — รับเฉพาะ `100000 / 010000 / 001000 / 000100 /
000010 / 000001 / 000000` (**ไม่มี `111111`**) ทดสอบทีละรูปแบบ เริ่มด้วย clear
watchdog จำกัดเวลา — ตรวจ **ลำดับจุดเชิงตรรกะ** เท่านั้น ไม่ได้ยืนยันหมายเลข
GPIO
